import { render } from "@testing-library/react";
import { ListLabels } from "./ListLabels";

describe("List Labels Component", () => {
  it("should render", () => {
    render(<ListLabels />);
  });
});
